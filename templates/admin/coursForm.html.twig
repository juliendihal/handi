{% extends 'base.html.twig' %}
{% block stylesheets %}<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">{% endblock %}
{% block body %}

{{ form_start(CoursForm , {attr: {novalidate: 'novalidate'}}) }}

    <div>
    {{ form_label(CoursForm.libelle ) }}
    {{ form_widget(CoursForm.libelle) }}
    </div>

    <div>
        {{ form_label(CoursForm.description ) }}
        {{ form_widget(CoursForm.description) }}
    </div>
    <div>
        {{ form_label(CoursForm.start ) }}
        {{ form_widget(CoursForm.start) }}
    </div>
    <div>
        {{ form_label(CoursForm.end ) }}
        {{ form_widget(CoursForm.end) }}
    </div>

<div>
    {{ form_label(CoursForm.fichier , 'fichier', {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(CoursForm.fichier, {'attr' : {'class': ' form-control'}}) }}
</div>
    <div>
    {{ form_widget(CoursForm.submit  , {'attr' : {'class': ' btn btn-primary mt-2'}, 'label': 'enregistrer'}) }}
    </div>
{{ form_end(CoursForm , {attr: {novalidate: 'novalidate'}}) }}


{% endblock %}